package me.angrybyte.dozetest

import com.google.firebase.messaging.FirebaseMessagingService

/**
 * Handles Firebase Cloud Messages/Notifications in foregrounded apps, receives data payload, it can send upstream messages, etc. Basically everything that
 * a simple background FCM receiver couldn't do.
 */
class FirebaseCloudReceiver : FirebaseMessagingService() {



}
